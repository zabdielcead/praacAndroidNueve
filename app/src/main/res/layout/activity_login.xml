<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".LoginActivity">

    <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:inputType="textEmailAddress"
            android:ems="10"
            android:id="@+id/edTextEmail"
            android:hint="email"
            app:layout_constraintTop_toTopOf="parent" app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="41dp" app:layout_constraintEnd_toEndOf="parent"/>
    <EditText
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:inputType="textPassword"
            android:ems="10"
            android:hint="password"
            android:id="@+id/edTextPassword"
            app:layout_constraintStart_toStartOf="@+id/edTextEmail" android:layout_marginBottom="23dp"
            app:layout_constraintBottom_toTopOf="@+id/switchRemember"/>
    <Switch
            android:text="Remember"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/switchRemember"
            app:layout_constraintTop_toTopOf="parent" android:layout_marginTop="189dp" android:layout_marginEnd="25dp"
            android:layout_marginRight="25dp" app:layout_constraintEnd_toEndOf="parent"/>
    <Button
            android:text="Login"
            android:layout_width="164dp"
            android:layout_height="46dp"
            android:id="@+id/btnLogin"
            android:layout_marginTop="24dp"
            app:layout_constraintTop_toBottomOf="@+id/switchRemember"
            app:layout_constraintEnd_toStartOf="@+id/switchRemember"/>
</android.support.constraint.ConstraintLayout>